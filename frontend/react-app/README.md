# Frontend

Simple React app for uploading orders and payments, viewing dashboard insights, and signing in.

## Run locally

```bash
cd frontend/react-app
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Notes

- The app talks to the backend API configured in the environment.
- Make sure the backend is running before using the app.

